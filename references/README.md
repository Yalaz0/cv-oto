# Private visual reference

Place the user-supplied CV at `source-cv.pdf`. This directory's assets are
excluded from Git and must never be copied into `public/` or a public fixture.

Reviewed 2026-09-22: two portrait pages, Canva output, approximately
595.5 × 842.25 pt. Page 1: portrait/contact header, summary, education,
eight experience entries. Page 2: skills/language, five projects, references.
Preserve the blue rules, square photo, page allocation, and text hierarchy.
The original text extraction contains unusual character spacing: use the
rendered reference when calibrating the template, not extraction order.
